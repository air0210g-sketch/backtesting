# 核心依赖（watch_metric、data_loader、indicators、回测框架等）
pandas>=1.5.0
numpy>=1.21.0

# 回测与策略脚本（framework、run_struct、run_talib_pattern、analyze_kdj_frequency）
vectorbt>=0.26.0

# 定时与通知（framework 定时任务、Telegram 通知）
schedule>=1.2.0
requests>=2.28.0

# 形态识别（watch_metric 周线/日线形态、run_talib_pattern）
# 安装前需先装 C 库：macOS `brew install ta-lib`，Ubuntu 编译安装，Windows 见官方文档
TA-Lib>=0.4.0

# 图表（analyze_kdj_frequency 等）
plotly>=5.0.0

# ---------- 可选：仅下载/校验行情数据时需要 ----------
# longport-openapi  # 数据下载脚本 scripts/data_tools 使用
